import React from "react";
import { Component } from "react";

export class Boxes extends Component {
  render() {
    return (
        <div>
        <button onClick={""}> FIR </button>
        <button onClick={""}> FIR </button>
      </div>
    );
  }
}